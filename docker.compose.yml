version: "3"
services:
  db:
    image: postgres:latest
    container_name: postgres
    restart: always
    ports:
      - "5432:5432"
    enviroment:
      POSTGRES_USER: "UserName"
      POSTGRES_PASSWORD: "Password"